# @layermint/sdk-core

Shared core engine for LayerMint variant resolution, symbol-level fallback, and contract validation.

This package is consumed by framework adapters like `@layermint/sdk-vite` and `@layermint/sdk-webpack`.
