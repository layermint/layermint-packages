# LayerMint Packages Workspace

Standalone workspace for publishable LayerMint packages:

- `@layermint/sdk-vite`
- `@layermint/cli`
- `@layermint/shared-types`

## Commands

```bash
pnpm install
pnpm build
pnpm test
pnpm typecheck
```
